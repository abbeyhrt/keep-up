# Golang CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-go/ for more details
version: 2
jobs:
  build:
    docker:
      - image: circleci/golang:1.10

    working_directory: /go/src/github.com/abbeyhrt/keep-up

    steps:
      - checkout
      # Include a test results and junit reporter for coverage
      - run: go get github.com/jstemmer/go-junit-report

      - run:
          name: Run unit tests
          command: go test -cover -v ./graphql/... 2>&1 | go-junit-report > report.xml

      - store_test_results:
          path: report.xml

# workflows:
  # version: 2

  # graphql:
    # - build
